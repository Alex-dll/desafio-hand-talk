import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    margin: 24,
  },

  title: {
    fontSize: 24,
    fontWeight: "700",
    color: "#333",
    paddingVertical: 10,
  },
  areaTitle: {
    marginBottom: 24,
  },
});

export default styles;
