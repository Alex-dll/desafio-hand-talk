import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    backgroundColor: "#fff",
    height: 25,
    borderRadius: 2,
    flex: 1,
    paddingHorizontal: 10,
    marginHorizontal: 3,
  },
});
