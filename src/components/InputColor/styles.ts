import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    backgroundColor: "#fff",
    height: 25,
    borderRadius: 2,
    width: 125,
    paddingHorizontal: 10,
  },
});
